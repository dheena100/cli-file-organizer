[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cli-file-organizer"
version = "1.0.0"
description = "CLI File Organizer"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["organizer"]